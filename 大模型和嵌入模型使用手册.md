# 🧠 大语言模型统一调用客户端文档

本模块封装了对多种大语言模型（LLM）的调用，支持灵活配置和多种部署方式，适用于构建企业级、模块化的智能应用。

---

## 📦 模块结构

- `config.toml`：模型调用配置文件
- `llm_client.py`：大模型统一调用客户端
- FastAPI `startup`：配置注入
- Chat 使用示例：结合 `langchain` 流程式调用模型

---

## 🛠 配置文件说明（`config.toml`）

```toml
[llm-default]
model = "qwen"                          # 模型名称（如 qwen、deepseek、mistral）
deployment_type = "vllm"               # 部署类型（network | ollama | vllm）
api_key = ""                            # 网络模型需填写，ollama/vllm 可留空
base_url = "http://10.0.5.73:8000/v1"  # 模型服务地址（vllm 和 network 必需）
temperature = 0.5                      # 模型温度，控制输出随机性
max_token = 4096                       # 最大生成 token 数量
```

---

## ⚙️ FastAPI 启动配置注入

在 FastAPI 项目的 `startup` 阶段，通过配置文件初始化模型客户端：

```python
from llm_client import LLMClient

LLMClient.init({
    "model_name": settings.llm_default["model"],
    "deployment_type": settings.llm_default["deployment_type"],
    "temperature": settings.llm_default["temperature"],
    "max_token": settings.llm_default["max_token"],
    "base_url": settings.llm_default["base_url"]
})
```

---

## 🧩 LLMClient 功能介绍

### ✅ 初始化配置（可多命名空间）

```python
LLMClient.init(config: Dict, namespace="default")
```

- 可支持多个模型配置同时存在，通过命名空间隔离使用。
- 参数说明：
  - `model_name`: 模型名称
  - `deployment_type`: `network` / `ollama` / `vllm`
  - `temperature`: 生成温度
  - `max_token`: 最大输出 token
  - `base_url`: 本地或远程模型地址
  - `api_key`: 网络模型认证参数

---

### ✅ 获取模型实例

```python
client = LLMClient.get(namespace="default", temperature=0.7, max_token=2048)
```

根据当前命名空间，返回指定类型的模型客户端，底层支持：

| 类型    | 描述                           | base_url | api_key |
| ------- | ------------------------------ | -------- | ------- |
| network | 如 Qwen、DeepSeek 网络模型调用 | ✅        | ✅       |
| ollama  | 本地模型推理服务               | ❌        | ❌       |
| vllm    | 本地/远程 vllm 服务            | ✅        | ❌       |

---

## 🚀 使用示例

结合 `langchain` 使用统一接口完成一个智能问答交互：

```python
from langchain.prompts import ChatPromptTemplate

async def chat_model(user_input: str, name: str):
    prompt_template = ChatPromptTemplate.from_messages([
        ("system", "你是一个智能助手，你的名字是{name}"),
        ("human", "我的问题是{user_input}")
    ])

    chat = LLMClient.get()  # 获取默认模型实例
    chain = prompt_template | chat

    response = chain.invoke({
        "name": name,
        "user_input": user_input,
    })

    return response.content
```

---

## 💡 示例配置参考

### ✅ 网络模型（如 Qwen）

```python
LLMClient.init({
    "model_name": "qwen-max",
    "deployment_type": "network",
    "api_key": "your_api_key",
    "base_url": "https://dashscope.aliyun.com/api/v1",
    ...
})
```

### ✅ Ollama 本地模型

```python
LLMClient.init({
    "model_name": "qwen:7b",
    "deployment_type": "ollama",
    ...
})
```

### ✅ vLLM 本地/远程模型

```python
LLMClient.init({
    "model_name": "/data/models/Mistral-7B",
    "deployment_type": "vllm",
    "base_url": "http://localhost:8000/v1",
    ...
})
```

---

## ❗错误处理说明

- `LLM_INIT_NAMESPACE_ERROR`：命名空间未初始化
- `LLM_INIT_LOADER_ERROR`：不支持的模型部署类型

---

## 📌 注意事项

- 如果使用 `vllm` 模型部署，请确保服务遵循 OpenAI API 协议格式。
- `ollama` 模型无需 `base_url`，直接本地推理。
- 所有模型支持 `temperature` 和 `max_token` 自定义调用。

---

## 📁 未来扩展建议

- 支持并发池复用或长连接复用（例如 `aiohttp` + `async`）
- 增加模型健康检查与切换机制
- 多租户场景下支持动态命名空间注册与卸载

---

# 🔍 向量化嵌入系统使用指南

本模块提供了一套灵活可配置的文本嵌入与向量存储方案，支持从多种嵌入模型（Ollama、HuggingFace、TEI）获取向量，并将其存储至 Milvus 向量数据库中，适用于检索增强生成（RAG）类应用开发。

---

## 📦 模块功能概览

### ✅ 支持的功能组件

| 组件                     | 说明                                                      |
| ------------------------ | --------------------------------------------------------- |
| `EmbeddingClientFactory` | 嵌入模型客户端工厂，统一支持 Ollama、HuggingFace、TEI     |
| `EmbeddingMilvusClient`  | 对 LangChain 的 Milvus 向量存储封装，支持写入、查询、删除 |
| `TextSplitterFactory`    | 多种文本切割策略的工厂，包括中文优化切割器、目录切割器等  |

---

## ⚙️ 配置说明（INI 或 ENV）

```ini
[embedding-model]
source="ollama"  # 可选值：ollama | hf | tei
ollama_model="bge-large:latest"
ollama_base_url="http://0.0.0.0:11434"
hf_model=""
tei_endpoint=""

[milvus]
host = "localhost"
port = 19530
user = "root"
password = ""
db_name = "default"
```

---

## 🧠 嵌入模型使用方式

### 初始化嵌入模型（FastAPI 启动时执行）

```python
EmbeddingClientFactory.init({
    "source": "ollama",
    "ollama_model": "bge-large:latest",
    "hf_model": "",
    "tei_endpoint": ""
})
```

### 获取嵌入客户端实例

```python
embedding_fn = EmbeddingClientFactory.get_client()
```

支持的模型来源：

- `"ollama"` - 本地部署的 Ollama 模型
- `"hf"` - HuggingFace 模型名称
- `"tei"` - HuggingFace TEI 接口

---

## 🧾 文本切割器用法

通过 `TextSplitterFactory` 快速构建以下几种切割器：

| 类型                | 功能说明                                         |
| ------------------- | ------------------------------------------------ |
| `char`              | 基于字符长度简单切割                             |
| `recursive`         | 通用递归分段                                     |
| `fixed_recursive`   | 固定分隔符+递归切割                              |
| `chinese`           | 中文优化句子切割器                               |
| `chinese_recursive` | 中文语义递归分段                                 |
| `toc`               | 按照文档目录切割章节（适用于 Word/PDF 转换内容） |

### 示例：构建递归文本切割器

```python
text_splitter = TextSplitterFactory.create_splitter(
    splitter_type="recursive",
    config={"chunk_size": 1000, "chunk_overlap": 200}
)
```

---

## 📥 文档向量化并写入 Milvus

### 示例：处理网页内容并存入向量数据库

```python
async def add_web_content_to_vector(url: str):
    loader = WebBaseLoader(web_path=url)
    docs = loader.load()

    # 切割文档
    text_splitter = TextSplitterFactory.create_splitter("recursive", {
        "chunk_size": 1000, "chunk_overlap": 200
    })
    all_splits = text_splitter.split_documents(docs)

    # 嵌入 + 入库
    embedding_function = EmbeddingClientFactory.get_client()
    EmbeddingMilvusClient.from_documents(
        embedding_function=embedding_function,
        documents=all_splits,
        collection_name="test01"
    )
```

---

## 🔍 相似搜索

### Top-k 相似搜索

```python
results = EmbeddingMilvusClient.similarity_search(
    query="小米SU7的特点",
    embedding_function=embedding_fn,
    collection_name="test01",
    k=4
)
```

### 带相似度分数的搜索

```python
results_with_scores = EmbeddingMilvusClient.similarity_search_with_score(
    query="小米SU7的特点",
    embedding_function=embedding_fn,
    collection_name="test01",
    k=4
)
```

---

## ❌ 向量删除操作

### 删除指定 ID 或过滤条件的数据

```python
EmbeddingMilvusClient.delete_documents(
    collection_name="test01",
    ids=["uuid1", "uuid2"]
    # 或使用 expr="some_field == 'xxx'"
)
```

---

## 🛠️ 注意事项

- **Milvus 客户端不应持久化为单例**，建议每次调用创建新连接。
- 嵌入模型初始化应在系统启动阶段执行一次。
- `TextSplitterFactory` 中中文切割器默认带正则优化，适合复杂语料。
- `toc` 切割器仅适用于已经带有结构化标题信息的文本（如 Word 转 Markdown）。

---

## 📚 依赖组件

- [LangChain](https://github.com/langchain-ai/langchain)
- [Ollama](https://github.com/ollama/ollama)
- [Milvus](https://milvus.io/)
- [HuggingFace Transformers / Inference Endpoint](https://huggingface.co/inference-endpoints)
- FastAPI、Tortoise ORM、BeautifulSoup、pymilvus 等